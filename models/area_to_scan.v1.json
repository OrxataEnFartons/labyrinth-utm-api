{
  "title": "area_to_scan",
  "type": "object",
  "description": "",
  "properties": {
    "area_vertices": {
      "type": "array",
      "minItems": 3,
      "description": "Perimeter of the area to scan. At least a triangle.",
      "items": {
        "type": "array",
        "minItems": 2,
        "maxItems": 2,
        "description": "Waypoint",
        "items": {
          "type": "number"
        }
      }
    },
    "path": {
      "type": "array",
      "description": "The path to follow inside the area. Leaving it optional in case they want to design the scan path in the planner.",
      "items": {
        "$ref": "./waypoint.v1.json"
      }
    }
  },
  "required": [
    "area_vertices"
  ]
}